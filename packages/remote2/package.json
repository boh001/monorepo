{
  "name": "remote2",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 8082",
    "build": "tsc && vite build",
    "preview": "vite preview --port 8082",
    "prettier": "prettier --write src"
  }
}