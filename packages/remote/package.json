{
  "name": "remote",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 8081",
    "build": "tsc && vite build",
    "preview": "vite preview --port 8081",
    "prettier": "prettier --write src"
  }
}